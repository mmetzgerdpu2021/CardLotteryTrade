The solidity function sendCard() may exceed the gas limit with a larger deck of cards, but the basics 
of every function works.

This was tested on Ganache CLI with the command "ganache-cli --port 8545 --accounts 4 --seed project".
